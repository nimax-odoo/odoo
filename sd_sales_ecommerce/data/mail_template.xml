<?xml version="1.0" ?>
<odoo>

    <!-- Mail template are declared in a NOUPDATE block
         so users can freely customize/delete them -->

    <data>

       <record id="email_template_update_Sale_order" model="mail.template">
            <field name="name">Ecommerce: Actualizar cotizaciones de carritos abandonados</field>
            <field name="model_id" ref="sale.model_sale_order"/>
            <field name="email_from">{{ (object.company_id.email_formatted or user.email_formatted) }}</field>
            <field name="partner_to">{{ object.partner_id.id }}</field>
            <field name="subject">Cotización cancelada por vencimiento {{ object.name }}</field>
            <field name="description">Sent to customers with their invoices in attachment</field>
            <field name="body_html" type="html">
<div style="margin: 0px; padding: 0px;">
    <p style="margin: 0px; padding: 0px; font-size: 13px;">
        Estimado(a)
        <t t-if="object.partner_id.parent_id">
            <t t-out="object.partner_id.name or ''">Brandon Freeman</t> (<t t-out="object.partner_id.parent_id.name or ''">Azure Interior</t>),
        </t>
        <t t-else="">
            <t t-out="object.partner_id.name or ''">Brandon Freeman</t>,
        </t>
        <br /><br />
Le notificamos que tiene una cotización <span style="font-weight:bold;" t-out="object.name or ''">SO</span> en nuestro e-commerce, que fue cancelada por vencimiento.

        <br />
        Si desea continuar con la compra, por favor ingrese al siguiente enlace y genere una nueva cotización:
        <br />
        <a t-att-href="object.get_portal_url()">
            <t t-esc="object.get_portal_url()"/>
        </a>
        <br /><br />
        <t t-if="object.name"/>  

        No dudes en contactarnos si tienes alguna pregunta.
        <t t-if="not is_html_empty(object.invoice_user_id.signature)">
            <br /><br />
            <t t-out="object.invoice_user_id.signature or ''">--<br/>Mitchell Admin</t>
        </t>
    </p>
</div>
            </field>
            <field name="report_template_ids" eval="[]"/>
            <field name="lang">{{ object.partner_id.lang }}</field>
            <field name="auto_delete" eval="True"/>
        </record>
        </data>
</odoo>
