<?xml version="1.0" encoding="utf-8"?>
<odoo>
<!-- Add action entry in the Action Menu for Partners -->
    <record id="as_partner_form_view" model="ir.ui.view">
        <field name="name">as.partner.form</field>
        <field name="model">res.partner</field>
        <field name="inherit_id" ref="base.view_partner_form"/>
        <field name="priority">50</field>
        <field name="arch" type="xml">
            <xpath expr="//field[@name='vat']" position="after">
                <field name="as_partner_type"/>
            </xpath>
            <page name="internal_notes" position="after">
                <page name="vendor_parameter" string="Vendor Paramter">
                    <field name="tf_vendor_parameter_ids">
                        <list>
                            <field name="name"/>
                            <field name="partner_type"/>
                        </list>
                    </field>
                </page>
            </page>
        </field>
    </record>
    <record id="sd_view_partner_form" model="ir.ui.view">
        <field name="model">res.partner</field>
        <field name="inherit_id" ref="base.view_partner_form" />
        <field name="arch" type="xml">
            <xpath expr="//sheet" position="before">

                <field name="sd_block_user" widget="statusbar"/>
            </xpath>
            <xpath expr="//field[@name='followup_responsible_id']" position="after">

                <field name="sd_block_user"/>
            </xpath>
        </field>
    </record>

    <record id="sd_view_partner_tree" model="ir.ui.view">
        <field name="model">res.partner</field>
        <field name="inherit_id" ref="base.view_partner_tree" />
        <field name="arch" type="xml">
            <xpath expr="//field[@name='phone']" position="after">
                <field name="sd_block_user" widget="badge" decoration-success="sd_block_user == 'False'" decoration-danger="sd_block_user == 'True'"/>
            </xpath>
        </field>
    </record>

</odoo>