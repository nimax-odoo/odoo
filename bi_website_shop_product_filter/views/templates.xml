<?xml version="1.0" encoding="utf-8"?>
<!-- views/pricelist_view_inherit.xml -->
<odoo>
    <template id="pricelist_list_inherit" inherit_id="website_sale.pricelist_list">
        <xpath expr="//div[@t-attf-class]" position="replace">
            <t t-set="website_sale_pricelists" t-value="website.get_pricelist_available(show_visible=True)" />
            <div t-attf-class="o_pricelist_dropdown dropdown#{'' if website_sale_pricelists and len(website_sale_pricelists) &gt; 0 else ' d-none'} #{_classes}">
                <t t-set="curr_pl" t-value="website.get_current_pricelist()" />
                <a role="button" href="#" class="dropdown-toggle btn btn-light border-0 px-0 text-muted align-baseline" data-toggle="dropdown">
                    <t t-esc="curr_pl and curr_pl.name or ' - '" />
                </a>
                <div class="dropdown-menu" role="menu">
                    <t t-foreach="website_sale_pricelists" t-as="pl">
                        <a role="menuitem" t-att-href="'/shop/change_pricelist/%s' % pl.id" class="dropdown-item">
                            <span class="switcher_pricelist" t-att-data-pl_id="pl.id" t-esc="pl.name" />
                        </a>
                    </t>
                </div>
            </div>
        </xpath>
    </template>
</odoo>
